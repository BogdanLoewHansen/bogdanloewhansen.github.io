# Template examples (not published)

Pages that shipped with the Academic Pages template, kept as reference for
front matter and layout usage. Jekyll ignores any top-level directory whose
name starts with an underscore unless it is a defined collection or listed
under `include:` in `_config.yml`, so nothing here is built or served.

To bring one back, move it into `_pages/` and check its `permalink:`.
